import { resolvers } from "./resolvers";
import { type } from "./types";
import { queries } from "./query";
import { mutations } from "./mutation";

export const User = { resolvers, type, queries, mutations };
